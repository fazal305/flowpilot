# Dummy values for the test suite only — never used for anything real, safe
# to commit. Loaded via `node --env-file=.env.test` in the test script.
JWT_SECRET=test-only-secret-never-used-outside-the-test-suite
NODE_ENV=test
